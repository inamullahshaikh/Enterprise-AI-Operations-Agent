# MCP examples

A small sample MCP server (e.g., "ticketing") used to demo Relay's bring-your-own-tools
flow: plugging in a server Relay has never seen and having the agent use it immediately,
with no code changes. See docs/system-design.md section 6.4 and Phase 6.

Implemented in Phase 6.
