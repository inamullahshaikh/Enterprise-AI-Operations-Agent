"""Capability taxonomy (section 7.1, Phase 2) and resolver (section 7.2, Phase 3 —
`relay_core.capabilities.resolver`). The MCP/OpenAPI capability tagger and admin-configurable
binding priorities (the rest of section 7) are Phase 6+, once there's a UI and tool sources
that need them (docs/adr/0009)."""
